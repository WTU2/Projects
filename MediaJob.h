// #ifndef MEDIAJOB_H
// #define MEDIAJOB_H
// #include "QString"

/*class MediaJob
{
    public:
        MediaJob(const QString& inputPath, const QString& outputPath);
        void start(); // starting the conversion
        int progress(); // progress bar 0-100
    private:
        QString inputPath;
        QString outputPath;
        int progress_;
};

#endif // MEDIAJOB_H*/
